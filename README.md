# Retail Data Analytics Pipeline — Microsoft Fabric

An end-to-end data engineering and analytics project built on **Microsoft Fabric**, implementing the **Medallion Architecture** (Bronze → Silver → Gold), **dimensional modeling** (Star Schema with surrogate keys), and a **Power BI** reporting layer.

## Architecture
